Store icon sets (SVGs, icon fonts) here. Recommended structure:

/icons/ui/  (SVG icons for UI)
/icons/social/  (SVG icons for social links)

Use inline SVGs or reference with <img src="/assets/icons/ui/close.svg" alt="Close"/>